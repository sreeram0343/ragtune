apiVersion: v1
kind: Service
metadata:
  name: ragtune-service
  namespace: ragtune-prod
  labels:
    app: ragtune-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ragtune-api
